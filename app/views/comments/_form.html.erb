<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body, placeholder: "Your comment", rows: 20, cols: 150 %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

