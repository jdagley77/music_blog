<div id="comment">
	<%= form_for([@post, @post.comments.build]) do |f| %>
	  <p>
	    <%= f.text_area :body, placeholder: "Add a comment", rows: 10 %>
	  </p>
	  <p>
	    <%= f.submit "Save Comment"%>
	  </p>
	<% end %>
</div>